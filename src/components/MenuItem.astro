---
export interface Props {
  item: {
    title: string;
    url: string;
  };
  current: string;
}
const { item, current } = Astro.props;
---

<li class="flex justify-center text-blue-800">
  <a href={item.url} class={current === item.url.slice(1) ? "underline" : ""}>
    {item.title}
  </a>
</li>
